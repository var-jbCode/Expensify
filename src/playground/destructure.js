const address = [];


const [, weeeyups = 'londonshire'] = address

console.log(`you are in ${weeeyups}`)

const item = ['PSL', 'wom', '£1.5', '£7', '£2.2']

const [coffeeType, temp, , medPrice] = item

console.log(`A ${coffeeType} ${temp} is ${medPrice}`)
